/**
 * §B1 — the approved Figma frame has no hero proof chips, but the brief calls for
 * them. The component is built and ready; this flag defaults to OFF to match the
 * frame, and can be switched on if Zac wants them.
 */
export const SHOW_HERO_CHIPS = false
