/**
 * The Shopify window is the Figma export (dash-sidebar / dash-panel /
 * dash-neworder). The hand-built HTML/CSS recreation is still in the repo —
 * see DashboardHtml in src/concepts/shared/Dashboard.tsx — and flipping this to
 * true swaps it back in on both concepts, in case we want the live version
 * again.
 */
export const USE_HTML_DASHBOARD = false
